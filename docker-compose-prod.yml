version: "3"
services:
  blog-react:
    image: blog-react:latest
    build:
      context: .
      dockerfile: Dockerfile.prod
      args:
        - NODE_ENV=production
    ports:
      - "8080:80"
    env_file:
      - ./.env
    container_name: blog
    
    